<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Override the search result item template to include author and publisher as separate lines -->
        <template id="search_result_item" inherit_id="website.one_hybrid" name="Search Result with Author and Publisher">
            <xpath expr="//div[hasclass('o_search_result_item_detail', 'px-3')]" position="inside">
                <small t-if="result.get('x_author')" class="text-muted d-block">
                    <t t-out="result.get('x_author')"/>
                </small>
                <small t-if="result.get('x_publisher')" class="text-muted d-block">
                    <t t-out="result.get('x_publisher')"/>
                </small>
            </xpath>
        </template>

        <!-- Override the product tile template to show author and publisher next to description -->
        <template id="product_tile_author_publisher" inherit_id="website_sale.products_item" name="Product Tile with Author and Publisher">
            <xpath expr="//div[hasclass('oe_subdescription_wrapper')]/div[hasclass('oe_subdescription', 'text-muted', 'small')]/div" position="after">
                <div t-if="product.author_line_ids or product.x_publisher_id or product.x_author or product.x_publisher" class="o_wsale_product_author_publisher small text-muted mt-1">
                    <t t-if="product.author_line_ids">
                        <t t-foreach="product.author_line_ids.sorted('sequence')" t-as="line">
                            <a t-attf-href="/shop/author/{{request.env['ir.http']._slug(line.author_id)}" class="text-muted" t-field="line.author_id.name"/>
                            <t t-if="not line_last">, </t>
                        </t>
                    </t>
                    <t t-elif="product.x_author">
                        <span t-out="product.x_author"/>
                    </t>
                    <t t-if="product.x_publisher_id">
                        <t t-if="product.author_line_ids or product.x_author"> · </t>
                        <a t-attf-href="/shop/publisher/{{request.env['ir.http']._slug(product.x_publisher_id)}" class="text-muted" t-field="product.x_publisher_id.name"/>
                    </t>
                    <t t-elif="product.x_publisher">
                        <t t-if="product.x_author"> · </t>
                        <span t-out="product.x_publisher"/>
                    </t>
                </div>
            </xpath>
        </template>

        <!-- Override the searchbar input to include author and publisher display toggles -->
        <template id="searchbar_with_author_publisher" inherit_id="website.website_search_box_input" name="Searchbar with Author and Publisher">
            <xpath expr="//input[@name='search']" position="attributes">
                <attribute name="t-att-data-display-author">display_author or 'true'</attribute>
                <attribute name="t-att-data-display-publisher">display_publisher or 'true'</attribute>
            </xpath>
        </template>
    </data>
</odoo>
